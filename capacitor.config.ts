import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coach.friend.app',
  appName: 'coach-friend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
