import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coachfriend.app',
  appName: 'coach-friend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
